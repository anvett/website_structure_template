/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://likeatedigital.anvetcard.com', // Reemplaza con tu subdominio real
  generateRobotsTxt: true, // Genera un archivo robots.txt
};

module.exports = config;
